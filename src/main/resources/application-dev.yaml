spring:
  datasource:
    url: jdbc:mysql://dev-db-server:3306/my_bank_db
    username: dev_user
    password: dev_password123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

server:
  port: 8080

logging:
  level:
    root: INFO
    com.example.mybankdemoapp: INFO
    org.springframework.web: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/my-bank-demo-app.log
    max-size: 10MB
    max-history: 30
